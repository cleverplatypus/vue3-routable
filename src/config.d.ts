import type { RoutingConfig } from './types';
export declare const DEFAULT_ROUTING_CONFIG: RoutingConfig;
export declare function createRoutingConfig(config?: Partial<RoutingConfig>): RoutingConfig;
declare const _default: RoutingConfig;
export default _default;
